# ==============================================================================
# BACKEND (Spring Boot) — Variables de Entorno
# ==============================================================================
# Copia este archivo a backend/.env para desarrollo local o exporta en el sistema:
# cp .env.example .env

# Conexión MySQL
DB_URL=jdbc:mysql://localhost:3306/quehaypahacer?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=pepe

# Azure Entra ID
AZURE_TENANT_ID=1abed6ed-c70a-4381-b9fe-0a67d2f0745e
AZURE_CLIENT_ID=36bb5da3-29f4-4386-988b-244a844b285c

# CORS (Orígenes autorizados)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:80,http://localhost

# Servidor
SERVER_PORT=8080
